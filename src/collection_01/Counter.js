// fix this component so it works as expected
const counter = () => {
  // clicks on buttons should increment/decrement the value 0
  return (
    <div>
      hello
      <p>0</p>
      <button>increment</button>
      <button>decrement</button>
    </div>
  );
};

export default counter;
